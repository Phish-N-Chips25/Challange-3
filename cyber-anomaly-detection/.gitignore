# ── Secrets ───────────────────────────────────────────────────────────────────
.env
*.env

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── Large binary artifacts (regenerable by running the notebooks) ─────────────
# Feature matrices and label arrays (download via setup_data.py from HF Hub)
*.npy
# Fitted sklearn objects (scalers, encoders, pipelines)
*.joblib
# Pickled data
*.pkl
!notebooks/checkpoints/seq/seq_chains_m.pkl
# PyTorch checkpoints
*.pt
!notebooks/checkpoints/models/autoencoder/word2vec/best.pt
!notebooks/checkpoints/models/seq/seq_ae_word2vec.pt
!notebooks/checkpoints/models/seq/seq_hpo_best_init.pt
# Gensim trained models (Word2Vec, FastText)
*.model
# Optuna SQLite study databases
*.db

# ── OS / IDE ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
